import { PrefixIconDirective } from './prefix-icon.directive';

describe('PrefixIconDirective', () => {
  it('should create an instance', () => {
    const directive = new PrefixIconDirective();
    expect(directive).toBeTruthy();
  });
});
