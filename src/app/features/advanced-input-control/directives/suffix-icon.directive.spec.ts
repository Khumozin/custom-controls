import { SuffixIconDirective } from './suffix-icon.directive';

describe('SuffixIconDirective', () => {
  it('should create an instance', () => {
    const directive = new SuffixIconDirective();
    expect(directive).toBeTruthy();
  });
});
